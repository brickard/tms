%h1 Listing employers

%table
  %tr
    %th Employee
    %th Company name
    %th Start date
    %th End date
    %th Phone number
    %th Job title
    %th Supervisor name
    %th Start salary
    %th End salary
    %th Reason for leaving
    %th
    %th
    %th

  - @employers.each do |employer|
    %tr
      %td= employer.employee_id
      %td= employer.company_name
      %td= employer.start_date
      %td= employer.end_date
      %td= employer.phone_number
      %td= employer.job_title
      %td= employer.supervisor_name
      %td= employer.start_salary
      %td= employer.end_salary
      %td= employer.reason_for_leaving
      %td= link_to 'Show', employer
      %td= link_to 'Edit', edit_employer_path(employer)
      %td= link_to 'Destroy', employer, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Employer', new_employer_path
