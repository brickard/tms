= content_for(:content_nav_heading) do
  = "Edit Expense"
= content_for(:content_nav) do
  = render :partial => 'shared/employee_navigation', :locals => { :@user => @employee }

.section_heading
  = "Edit Expense for #{@employee.full_name}"
= render :partial => "form"

