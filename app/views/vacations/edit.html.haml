= content_for(:content_nav_heading) do
  = "Edit Vacation"
= content_for(:content_nav) do
  = render :partial => 'shared/employee_navigation'

.section_heading
  = "Edit Vacation for #{@employee.full_name}"
= render :partial => "form"

