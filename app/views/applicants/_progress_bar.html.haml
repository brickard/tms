= javascript_tag "$(function() { $( \"#progressbar\" ).progressbar({ value: #{@progress_value}}); });"
#progress
  #progressbar
  - unless @step >= @total_steps
    %p.step_text You are currently on step #{@step} of #{@total_steps}.
  - else
    %p.step_text All steps complete!

%br/
