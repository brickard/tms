%table{ :class => 'section_content data_table' }
  %tr
    %th UserID (email)
    %th Name
    %th
    %th
    %th
    %th
  - @users.each do |user|
    %tr
      %td= user.email
      %td= user.full_name
      %td= link_to 'Show', employee_path(user)
      %td= link_to 'Print', employee_print_path(user), :target => '_new'
      %td= link_to 'Edit', edit_employee_path(user)
      %td= link_to 'Delete', employee_path(user), :confirm => 'Are you sure?', :method => :delete
%br/
